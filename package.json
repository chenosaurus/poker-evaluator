{
  "name": "poker-evaluator-ts",
  "version": "1.0.0",
  "description": "A library to evaluate 3, 5 or 7 card poker hands (port of poker-evaluator js)",
  "main": "index.ts",
  "scripts": {
    "start": "mkdir -pv dist/data && cp data/HandRanks.dat dist/data/HandRanks.dat && tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^13.1.6",
    "jest": "^24.9.0",
    "typescript": "^3.7.4"
  },
  "engines": {
    "node": ">= 0.6.0"
  }
}
